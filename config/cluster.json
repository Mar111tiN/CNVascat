{
  "__default__": {
    "t": "00:45",
    "mem": "1000",
    "nodes": 1,
    "partition": "debug",
    "threads": "{threads}",
    "name": "{rule}:{wildcards.sample}.{wildcards.type}"
  },
  "query_normal": {
    "t": "00:55",
    "mem": 500,
    "name": "{rule}:{wildcards.sample}.{wildcards.normal}.{wildcards.chrom}"
  },
  "query_tumor": {
    "t": "00:25",
    "mem": 500,
    "name": "{rule}:{wildcards.sample}.{wildcards.tumor}.{wildcards.chrom}"
  },
  "combine_query": {
    "t": "00:10",
    "mem": 2000,
  },
  "ascat": {
    "t": "00:300",
    "mem": 2000,
    "name": "{rule}:{wildcards.sample}"
  }
}